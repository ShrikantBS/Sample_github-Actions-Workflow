name: "Hello World workflow"

on:
  push:
    branches:
        -master
        -production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Prints Hello World!"
        run: echo "Hello World!!"